services:
  telegram-voice-transcriber:
    build: .
    container_name: telegram-voice-transcriber
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
      - ./storage:/app/storage
      - ./models:/root/.cache/huggingface
